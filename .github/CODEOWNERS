# Each line is a file pattern followed by one or more owners.
# Later match takes precedence over earlier rule.

# Global rule set for everything, and special cases exclude later
# Code owners are deliberately omitted so that
# no one is auto-requested, emailed, notified, or round-robin assigned.
