---
info:
  name: Create Agent Get Workflows Query
  type: http
  seq: 1

http:
  method: POST
  url: "{{ik_api_url_prefix}}/configs/v1/knowledge-queries"
  headers:
    - name: Content-Type
      value: application/json
  body:
    type: json
    data: |-
      {
          "name": "get-agent-workflows",
          "display_name": "Get Agent Workflows",
          "description": "Given an agent id return all of the workflows and the relationships",
          "project_id": "{{project_id}}",
          "policy_id": "{{get_agent_workflows_policy}}",
          "query": "{ \"nodes\": [ \"agent\" ], \"relationships\": [  ], \"aggregate_values\": [ \"workflow_list\" ] }",
          "status": "ACTIVE"
      }
  auth: inherit

runtime:
  variables:
    - name: get_agent_workflows_policy
      value: gid:AAAAFn8x970x80CPrzeCXVe_eYU

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
