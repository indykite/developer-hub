---
info:
  name: Execute Get Stock Trade Threshold Query
  type: http
  seq: 3

http:
  method: POST
  url: "{{ik_api_url_prefix}}/contx-iq/v1/execute"
  headers:
    - name: Content-Type
      value: application/json
    - name: X-IK-ClientKey
      value: "{{agent_token}}"
  body:
    type: json
    data: |-
      {
          "id": "get-stock-trade-threshold",
          "input_params": {
            "customer_external_id": "rebecca"
          }
      }
  auth:
    type: bearer
    token: "{{user_token}}"

runtime:
  variables:
    - name: get_agent_workflows_policy
      value: gid:AAAAFn8x970x80CPrzeCXVe_eYU

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
